import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maruf.expenseledger',
  appName: 'Expense Ledger',
  webDir: 'dist/public'
};

export default config;
